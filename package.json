{
  "name": "dice-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"pnpm --filter web dev\" \"pnpm --filter api start:dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
